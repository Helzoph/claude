{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "helzoph-claude-marketplace",
  "description": "Personal Claude Code plugin marketplace",
  "owner": {
    "name": "helzoph"
  },
  "plugins": [
    {
      "name": "rationale-toolkit",
      "description": "Rationale-first coding style and outcome-verification discipline: explain the why before the what, and prove changes work before calling them done. Ships an optional ELI15 output style for plain-language explanations that always state what changed and what the user must do next, and a hook that blocks direct node_modules/.bin/ invocations.",
      "source": "./plugins/rationale-toolkit",
      "category": "development"
    },
    {
      "name": "dev-env-router",
      "description": "Directory-derived identity for local dev environments via Docker Compose + a single shared Traefik router: no manual port bookkeeping across projects and worktrees.",
      "source": "./plugins/dev-env-router",
      "category": "development"
    },
    {
      "name": "mise-toolchain",
      "description": "Project toolchain versions and cross-worktree env layering via mise: inherit the machine's own version preferences into mise.toml, and share one baseline .env across worktrees without copying secrets around.",
      "source": "./plugins/mise-toolchain",
      "category": "development"
    },
    {
      "name": "dev-credentials",
      "description": "A single machine-level file holding budget-capped dev credentials that an agent is explicitly allowed to read and use for testing, so real project secrets never have to be exposed to get a test run working.",
      "source": "./plugins/dev-credentials",
      "category": "development"
    },
    {
      "name": "emil-design-skills",
      "description": "Polished interface design, animation, motion review, and frontend tooling skills by Emil Kowalski.",
      "source": "./plugins/emil-design-skills",
      "category": "development"
    }
  ]
}
