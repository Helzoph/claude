# Personal machine config — kept locally only, never tracked in this
# marketplace repo (no plugin mechanism auto-injects these; see README.md).
# reference/global-claude.md is the tracked, public counterpart of ~/.claude/CLAUDE.md.
/setting.json
/CLAUDE.md
/.claude/
